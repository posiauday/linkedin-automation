anthropic>=0.40.0
requests>=2.31.0
python-dotenv>=1.0.0
PyYAML>=6.0

# Carousels only. Rendering slides to PDF drives headless Chromium:
#   pip install playwright && playwright install chromium
# Everything else works without it.
playwright>=1.40.0
